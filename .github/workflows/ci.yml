name: Run simple test

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Display CI message
        run: echo "Continuous Integration workflow is running"

      - name: Run simple test
        run: echo "Test completed successfully"
